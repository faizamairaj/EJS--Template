<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dynamic Table 🔢</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #cea0bb, #95bab6);
            min-height: 100vh;
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center flex-column p-5">
    
    <div class="bg-white rounded shadow p-5" style="min-width: 350px;">
        <h1 class="text-center text-secondary mb-4">Generate Table</h1>
        <form action="/" method="GET" class="mb-4">
            <div class="mb-3">
                <i><label for="num" class="form-label">Enter a number:</label></i>
                <input type="number" id="num" name="num" required class="form-control">
            </div>
            <button type="submit" class="btn btn-warning w-100">Generate</button>
        </form>

        <% if (!isNaN(num)) { %>
            <i><h2 class="text-center text-success">Table of <%= num %></h2></i>
            <table class="table table-bordered text-center table-striped mt-3">
                <tbody>
                    <% for(let i = 1; i <= 10; i++) { %>
                        <tr>
                            <td><%= num %> × <%= i %> = <%= num * i %></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>
